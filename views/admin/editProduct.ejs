<%-include('../layouts/adminHeader.ejs')%>

    <style>
        body {
            font-family: Arial, sans-serif;
            /* margin: 20px; */
        }

        form {
            max-width: 600px;
            margin: 40px auto
        }
    </style>
</head>

<body>
    
    
    
    <div class="admin_header">
        <div class="admin_logo">
            <img src="/assets/Frame.png" alt="milma-logo">
        </div>
        <div class="logout_button">
            <a href="/admin/logout">
                <button type="button" class="btn btn-danger">
                    Logout <i class="fas fa-sign-out-alt"></i>
                </button>
            </a>
        </div>
    </div>

    <div class="container-fluid">
        <h2 class="mb-4">Edit Product</h2>

    <form action="/admin/editProduct?id=<%=product._id%>"
        method="post">
        <div class="form-group">
            <label for="productName">Product Name:</label>
            <input type="text" name="productName" class="form-control" value="<%=product.productName %>" id="productName" placeholder="Enter product name">
        </div>
        <div class="form-group">
            <label for="price">Price:</label>
            <input type="text" name="price" class="form-control" value="<%=product.price %>" id="price" placeholder="Enter price">
        </div>
        <div class="form-group">
            <label for="stock">Stock:</label>
            <input type="text" name="quantity" class="form-control" value="<%=product.quantity %>" id="stock" placeholder="Enter stock">
        </div>
        <div class="form-group">
            <label for="category">Category:</label>
            <select class="form-control" name="category" id="category">
                <% category.forEach(function(category,index){ %>
                    <option value="<%=category.categoryName%>"><%=category.categoryName%>
                    </option>
                    <% }) %>
                
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Edit Product</button>
    </form>
    </div>

    <%-include('../layouts/adminFooter.ejs')%>
